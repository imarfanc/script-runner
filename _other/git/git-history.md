# Commit history

Generated by `deno task git:history` on 2026-08-15. 15 commits.

Regenerate rather than edit; Git is the source of truth.

## 2026-08-15

- `56d1ddf` ⭐🎨 add(theme): add a light/dark/system theme, following macOS appearance live — imarfanc
## 2026-08-14

- `593c8da` ⭐🛠️ add(setup): add a staged new-mac setup suite and group the script list by marker field — imarfanc
- `8b8c0b5` ⭐📁 add(workspace): let a run window show a script's source files and open them in the editor — imarfanc
- `35293cf` 🐛📐 repair(layout): let the workspace column fill remaining width instead of a fixed 960px — imarfanc
- `381d0ea` ⭐🖥️ add(workspace): render real terminal output and separate opening a window from running it — imarfanc
- `e6ba53e` ⭐📂 add(demo): add a demo suite of scripts covering every supported language — imarfanc
- `e375827` ♻️🔧 reshape(scripts): split repo-script config and terminal styling into their own modules — imarfanc
- `87846c5` 🧹🔤 simplify(icons): drop the Font Awesome dependency for hardcoded Nerd Font glyphs — imarfanc
- `1ca82f8` Revert "⭐📱 add(whatsapp): pivot repo into a scheduled WhatsApp send app" — imarfanc
- `f0ecdf5` Revert "⭐📤 add(uploads): let the composer attach ad-hoc files, not just outputs" — imarfanc
- `95eaf9e` ⭐📤 add(uploads): let the composer attach ad-hoc files, not just outputs — imarfanc
- `b4b3a8b` ⭐📱 add(whatsapp): pivot repo into a scheduled WhatsApp send app — imarfanc
## 2026-08-13

- `cd1cc91` ⭐🧰 add(tooling): ship dev script toolbelt and homebrew examples — imarfanc
- `3afc85e` 📚🖥️ add(examples): ship example script library and fix window drag — imarfanc
- `1b355e8` i — imarfanc
